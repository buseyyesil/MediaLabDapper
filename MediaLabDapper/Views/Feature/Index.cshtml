@model IEnumerable<MediaLabDapper.DTOs.FeatureDtos.ResultFeatureDto>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-header">
    <h3 class="page-title">Feature Listesi</h3>
</div>
<div class="row">
    <div class="col-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <a asp-action="CreateFeature" class="btn btn-primary mb-3">Yeni Ekle</a>
                <div class="table-responsive">
                    <table class="table">
                        <thead class="bg-light">
                            <tr>
                                <th>#</th>
                                <th>Başlık</th>
                                <th>Açıklama</th>
                                <th>İkon</th>
                                <th>İşlemler</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.FeatureId</td>
                                    <td>@item.Title</td>
                                    <td>@item.Description</td>
                                    <td><i class="@item.Icon"></i> @item.Icon</td>
                                    <td>
                                        <a asp-action="UpdateFeature" asp-route-id="@item.FeatureId" class="btn btn-warning btn-sm">Güncelle</a>
                                        <a asp-action="DeleteFeature" asp-route-id="@item.FeatureId" class="btn btn-danger btn-sm">Sil</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>