@model IEnumerable<MediaLabDapper.DTOs.DepartmentDtos.ResultDepartmentDto>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-header">
    <h3 class="page-title">Bölüm Listesi</h3>
</div>
<div class="row">
    <div class="col-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <a asp-action="CreateDepartment" class="btn btn-primary mb-3">Yeni Ekle</a>
                <div class="table-responsive">
                    <table class="table">
                        <thead class="bg-light">
                            <tr>
                                <th>#</th>
                                <th>Bölüm Adı</th>
                                <th>Açıklama</th>
                                <th>İşlemler</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.DepartmentId</td>
                                    <td>@item.DepartmentName</td>
                                    <td>@item.Description</td>
                                    <td>
                                        <a asp-action="UpdateDepartment" asp-route-id="@item.DepartmentId" class="btn btn-warning btn-sm">Güncelle</a>
                                        <a asp-action="DeleteDepartment" asp-route-id="@item.DepartmentId" class="btn btn-danger btn-sm">Sil</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>